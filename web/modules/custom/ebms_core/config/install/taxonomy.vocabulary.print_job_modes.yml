langcode: en
status: true
dependencies:
  enforced:
    module:
      - ebms_core
name: 'Print Job Modes'
vid: print_job_modes
description: 'Whether the print request is for a live job, a test, or just a report of what would be printed.'
weight: 0
