langcode: en
status: true
dependencies:
  config:
    - field.storage.taxonomy_term.field_text_id
    - taxonomy.vocabulary.hotels
  enforced:
    module:
      - ebms_core
id: taxonomy_term.hotels.field_text_id
field_name: field_text_id
entity_type: taxonomy_term
bundle: hotels
label: 'Text ID'
description: 'Used to specify the hotel at which the board member would prefer to stay.'
required: false
translatable: true
default_value: {  }
default_value_callback: ''
settings: {  }
field_type: string
